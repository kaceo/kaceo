---
layout: base

common_ext_css:
  - "https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css"
  - "https://unpkg.com/prismjs@1.20.0/themes/prism-okaidia.css"

common_ext_js:
  - "https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"

eleventyComputed:
  root_slug: "{{ '/' | url }}"
  top_menus:
  - name: About
    url: "{{ '/about/' | url }}"
  - name: Links
    url: "{{ '/links/' | url }}"
  - name: Catalog
    url: "{{ '/catalog/' | url }}"
  - name: Start
    url: "{{ '/start/' | url }}"


wop_menus:
  - name: About
    url: "{{ '/about/' | url }}"
    icon: camera
  - name: Links
    url: /links/
    icon: edit
  - name: Catalog
    url: /catalog/
    icon: school
  - name: Start
    url: /blog/
    icon: school

special_menus:
  - name: tv
    url: /tvhome/
    icon: brightness_3

---
include /bootstrap/prechrome.pug
.container-fluid
  | !{ content }
include /bootstrap/postchrome.pug

